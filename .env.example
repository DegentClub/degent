# Required: DALL-E 3 access
OPENAI_API_KEY=

# Optional: shared secret for /api/generate-frame. When set, every request
# must send it in the `x-atelier-key` header and per-IP rate limiting is off.
# When unset, the route falls back to 5 requests / 10 minutes per IP.
# ATELIER_API_KEY=
